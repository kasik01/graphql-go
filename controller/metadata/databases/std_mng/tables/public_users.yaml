table:
  name: users
  schema: public
event_triggers:
  - name: test
    definition:
      enable_manual: false
      insert:
        columns: '*'
      update:
        columns: '*'
    retry_conf:
      interval_sec: 10
      num_retries: 0
      timeout_sec: 60
    webhook: '{{GO_BACKEND_URL}}/notifications'
